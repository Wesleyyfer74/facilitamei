Options -Indexes
DirectoryIndex index.html

<IfModule mod_rewrite.c>
  RewriteEngine On

  RewriteCond %{HTTP_HOST} ^www\.facilitameibr\.com\.br$ [NC]
  RewriteRule ^ https://facilitameibr.com.br%{REQUEST_URI} [R=301,L]

  RewriteRule ^(\.env|\.env\..*|server\.js|backend\.log|package\.json|package-lock\.json|DEPLOYMENT\.md|HOSTINGER_DEPLOY\.md)$ - [F,L,NC]
  RewriteRule ^(data|database|docs|scripts|node_modules|\.git|\.agents|\.codex)(/|$) - [F,L,NC]
  RewriteRule ^areaadmin/?$ areaadmin/index.html [L]

  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</IfModule>

<IfModule mod_headers.c>
  Header always set X-Content-Type-Options "nosniff"
  Header always set X-Frame-Options "DENY"
  Header always set Referrer-Policy "strict-origin-when-cross-origin"
  Header always set Permissions-Policy "camera=(), microphone=(), geolocation=(), payment=()"
</IfModule>
